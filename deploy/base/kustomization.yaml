apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Shared manifests for every environment. Add Deployment/Service/Ingress per
# component here as the React app and Kotlin services land, e.g.:
#   - web/deployment.yaml
#   - services/<name>/deployment.yaml
resources: []
