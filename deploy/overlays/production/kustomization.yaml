apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: overstapelen-production

resources:
  - ../../base

# Image tags below are rewritten by the promotion workflows (staging/production)
# or by the Argo CD ApplicationSet (preview). Do not edit them by hand.
images: []
